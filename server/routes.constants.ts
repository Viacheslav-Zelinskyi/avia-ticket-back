export const CLIENT_URL = {
  home: "/home",
  tickets: "/tickets",
  myTickets: "/mytickets",
  about: "/about",
};

export const ROOT_URL = "/";

export const BASE_URL = "/api";

export const USERS_URL = "/users";

export const TICKETS_URL = "/tickets";

export const LOGOUT_URL = "/logout";

export const SIGNUP_URL = "/signup";

export const LOGIN_URL = "/login";

export const REFERSH_TOKEN_URL = "/refresh-token";

export const TICKETS_FULL_ROUTE = BASE_URL + TICKETS_URL;

export const SIGNUP_FULL_ROUTE = BASE_URL + USERS_URL + SIGNUP_URL;

export const LOGIN_FULL_ROUTE = BASE_URL + USERS_URL + LOGIN_URL;
